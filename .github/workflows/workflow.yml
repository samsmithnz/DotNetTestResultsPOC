name: CI/CD
on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
jobs:
  build:
    name: Build job
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: .NET test
      run: dotnet test src/DotNetTestResultsPOC.Tests/DotNetTestResultsPOC.Tests.csproj -c Release  -l:"trx;LogFileName=${{ github.workspace }}/TestOutput.xml"
    - run: |
        ls ${{ github.workspace }}/
        echo ${{ github.workspace }}
    - name: Output results
      #if: runner.OS == 'Linux'
      if: 0 == 1
      shell: pwsh
      run: |
        [xml]$result = Get-Content -Path ${{ github.workspace }}/TestOutput.xml
        #$result.TestRun.ResultSummary.Counters.passed
        #$result.TestRun.ResultSummary.Counters.failed
        echo "Test results" >> $GITHUB_STEP_SUMMARY
        #echo "" >> $GITHUB_STEP_SUMMARY # this is a blank line
        #echo "- Total: $($result.TestRun.ResultSummary.Counters.total)" >> $GITHUB_STEP_SUMMARY
        #echo "- Passed ✅: $($result.TestRun.ResultSummary.Counters.passed)" >> $GITHUB_STEP_SUMMARY
        #echo "- Failed ❌: $($result.TestRun.ResultSummary.Counters.failed)" >> $GITHUB_STEP_SUMMARY

    - name: Generate list using Markdown
      run: |
        echo "This is the lead in sentence for the list" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY # this is a blank line
        echo "- Lets add a bullet point" >> $GITHUB_STEP_SUMMARY
        echo "- Lets add a second bullet point" >> $GITHUB_STEP_SUMMARY
        echo "- How about a third one?" >> $GITHUB_STEP_SUMMARY

